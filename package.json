{
  "name": "morpheus-ai-tools",
  "version": "1.0.0",
  "description": "Deployment tools for Morpheus AI",
  "scripts": {
    "publish:env": "node tools/publish-env.js",
    "build:server": "cd server && npm install && npm run build:prod",
    "build:server-test": "cd server && npm install && npm run build:test",
    "publish:server": "npm run build:server && node tools/publish-server.js",
    "publish:server-test": "npm run build:server-test && node tools/publish-server.js --config=../config.test.json",
    "build:mobile": "cd web-mobile && npm install --legacy-peer-deps && npm run build:h5",
    "build:mobile-test": "cd web-mobile && npm install --legacy-peer-deps && npm run build:h5-test",
    "publish:mobile": "npm run build:mobile && node tools/publish-mobile.js",
    "publish:mobile-test": "npm run build:mobile-test && node tools/publish-mobile.js --config=../config.test.json",
    "publish:nginx": "node tools/publish-nginx.js"
  },
  "dependencies": {
    "node-ssh": "^13.2.1",
    "ssh2-sftp-client": "^12.1.0"
  }
}